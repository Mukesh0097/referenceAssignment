#include<stdio.h>

void  main(){
    int A ,B;
    printf("enter the value: ");
    scanf("%d",&A);
    printf("enter the value: ");
    scanf("%d",&B);
    (A == B)?printf("both number are equal"):printf("numbers are not equal");//ternary operator
}